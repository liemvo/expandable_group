name: example
description: An example of List Expandable.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3

dev_dependencies:
  expandable_group:
    path: ../
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
